[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"
  directory = "netlify/functions"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=()"
    X-XSS-Protection = "0"

# API routes — proxy to functions (Config path also serves these)
[[redirects]]
  from = "/api/opportunities"
  to = "/.netlify/functions/get-opportunities"
  status = 200
  force = true

[[redirects]]
  from = "/api/scan-status"
  to = "/.netlify/functions/get-scan-status"
  status = 200
  force = true

# SPA fallback — must stay last; force=false so real assets win
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
